function isolatePhrase(inputText, idx, phrase) {
  // Takes in some information and returns the specific text to be turned into a madlib
  return inputText;
  // returns the phrase and its filler text
}

export default isolatePhrase;
